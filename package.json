{
  "name": "valheim-aws",
  "version": "1.0.0",
  "description": "An AWS hosted Valheim game server",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "src"
  ],
  "dependencies": {
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "aws-sdk": "^2.874.0"
  }
}
